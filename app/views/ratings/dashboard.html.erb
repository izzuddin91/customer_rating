 <script>
 $(function (){
  $("#result").autocomplete({
    source: "/autocomplete_result",
    success: function( data ) {
      array = $.map(data.value, function (item) {
        return {
          label: item.Name,
        }
      });
    }
  });

  
});
 </script>

 <div class="container">
  <h3>hello, <%= @user %></h3>

<div class="container">
  <div class="col-md-6">
  <h2>Masuk shipper baru</h2>
  <%= form_for(@customer) do |f| %>
  <%= f.label :nama_shipper %> <br>
  <%= f.text_field :name %>
  <div class="action">
    <%= f.submit %>
  </div>
  <%end%>
</div>
  <div class="col-md-6">
    <h2>Atau cari shipper</h2>
  <%= form_tag("/searches", :method => "get") do %>
  <%= label_tag(:result, "Cari:") %>
  <%= text_field_tag(:result) %>
  <%= submit_tag("Search") %>
  <% end %>
  </div>
</div>


  <h1>Senarai Shipper/customer</h1>
  <table border="1" style="width:100%; text-align:center;">
    <tr>
      <td>Senarai Shipper</td>
      <td>Bilangan komen</td>
    </tr>
    <tr>
      <% @customers.each do |x| %>
      <td><%= link_to x.name, x %></td>
      <td><%= x.comments.count.to_i %></td>
    </tr>
    <%end%>
  </table>







</div>






